name: 'publish-storybook-ui'

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'packages/ui/src/**'

jobs:
  chromatic:
    name: 'Run Chromatic'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          filter: tree:0
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun ci

      - uses: chromaui/action@latest
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          onlyChanged: true
          token: ${{ secrets.GITHUB_TOKEN }}
          workingDir: packages/ui
          buildCommand: nx build-storybook ui
          